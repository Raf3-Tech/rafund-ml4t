# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual Environment
venv/
.venv/
env/

# Environment variables (SECRETS)
.env

# VS Code
.vscode/

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# Database dumps / temp
*.sqlite3
# Keep schema in repo for onboarding
!data/schema.sql

# Jupyter
.ipynb_checkpoints/

# Signal research notebooks (scratch/exploratory, not part of the app)
signal research/

# Misc
*.tmp

# MLflow artifacts
mlruns/
mlflow.db

# Generated leaderboard report — written by monitoring/leaderboard.py, not hand-maintained
LEADERBOARD.md

# Kraken Prop app reference material (accessibility dumps, screenshots) — reflects a
# specific account's own screens, kept locally only, never committed
docs/mobile_nav/

# Pytest coverage artifacts
.coverage
coverage.xml
htmlcov/

# Local model-training output (feature importance plots, tmp JSON)
tmp/

# Dashboard asset bundle — built by monitoring/static/build.sh (docker build
# or local `npx esbuild`), never checked in
monitoring/static/dist/
node_modules/

# Claude Code local tool state (settings, worktrees) — not part of the app
.claude/
